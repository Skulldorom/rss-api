name: Build, Test, and Push Docker image

on:
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set lowercase repo owner
        run: echo "REPO_OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

      - name: Set image tag
        id: meta
        run: echo "image_tag=${{ github.event.repository.name }}:${GITHUB_SHA}" >> $GITHUB_OUTPUT

      - name: Build Docker image
        run: docker build -t ${{ github.event.repository.name }}:${GITHUB_SHA} .

  push:
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set lowercase repo owner
        run: echo "REPO_OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

      - name: Build Docker image
        run: docker build -t ${{ github.event.repository.name }}:${GITHUB_SHA} .

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_DOCKER_PULL }}

      - name: Tag image for GHCR
        run: |
          docker tag ${{ github.event.repository.name }}:${GITHUB_SHA} ghcr.io/${REPO_OWNER_LC}/${{ github.event.repository.name }}:${GITHUB_SHA}
          docker tag ${{ github.event.repository.name }}:${GITHUB_SHA} ghcr.io/${REPO_OWNER_LC}/${{ github.event.repository.name }}:latest

      - name: Push image to GHCR
        run: |
          docker push ghcr.io/${REPO_OWNER_LC}/${{ github.event.repository.name }}:${GITHUB_SHA}
          docker push ghcr.io/${REPO_OWNER_LC}/${{ github.event.repository.name }}:latest
